title: Cassie Liang
description: Backend engineer — Java, Python, AI systems
markdown: kramdown
encoding: utf-8

exclude:
  - Gemfile
  - Gemfile.lock
  - vercel.json

defaults:
  - scope:
      path: ""
    values:
      layout: default
