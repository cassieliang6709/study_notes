---
title: "09 堆、设计、区间、数学与杂项完整答案"
---

# 09 堆、设计、区间、数学与杂项完整答案

说明：

- 本文按题目分节
- 每题包含：题型识别、题目含义、Python 代码、复杂度、思路来源、常见 Follow-up
- 这册内容杂，但做题时仍然要先识别模板

---

## 1. Kth Largest Element in an Array

**题型识别**

```text
小顶堆保留前 k 大 / Min Heap for Top-k
```

**题目含义**

维护一个大小为 `k` 的小顶堆：

- 堆里始终保留当前前 `k` 大元素
- 堆顶就是第 `k` 大

**Python 代码**

```python
import heapq
from typing import List

class Solution:
    def findKthLargest(self, nums: List[int], k: int) -> int:
        heap = []
        for x in nums:
            heapq.heappush(heap, x)
            if len(heap) > k:
                heapq.heappop(heap)
        return heap[0]
```

**时间复杂度**

`O(n log k)` 用堆；Quickselect 平均 `O(n)`。

**空间复杂度**

`O(k)`。

**怎么想到这个方法**

先看题型识别里的信号：这题本质上就是 `小顶堆保留前 k 大 / Min Heap for Top-k`。把题目翻译成这个模板后，再去套对应的不变量、状态定义或数据结构，就会更容易写出来。

**常见 Follow-up**

- 如果 `k` 很小，为什么堆通常比全排序更合适？
- 如果数据流持续到来，解法要怎么调整？

## 2. K Closest Points to Origin

**题型识别**

```text
Top-k / Heap Selection
```

**题目含义**

按点到原点的平方距离排序，取最小的 `k` 个即可。  
Python 里可以直接用 `heapq.nsmallest`。

**Python 代码**

```python
import heapq
from typing import List

class Solution:
    def kClosest(self, points: List[List[int]], k: int) -> List[List[int]]:
        return heapq.nsmallest(k, points, key=lambda p: p[0] * p[0] + p[1] * p[1])
```

**时间复杂度**

`O(n log k)`。

**空间复杂度**

`O(k)`。

**怎么想到这个方法**

先看题型识别里的信号：这题本质上就是 `Top-k / Heap Selection`。把题目翻译成这个模板后，再去套对应的不变量、状态定义或数据结构，就会更容易写出来。

**常见 Follow-up**

- 如果 `k` 很小，为什么堆通常比全排序更合适？
- 如果数据流持续到来，解法要怎么调整？

## 3. Top K Frequent Elements

**题型识别**

```text
频率统计 + Top-k / Frequency + Top-k
```

**题目含义**

先统计频率，再取前 `k` 个高频元素。  
Python 中 `Counter(...).most_common(k)` 很适合这题。

**Python 代码**

```python
from collections import Counter
from typing import List

class Solution:
    def topKFrequent(self, nums: List[int], k: int) -> List[int]:
        return [x for x, _ in Counter(nums).most_common(k)]
```

**时间复杂度**

`O(n log k)`。

**空间复杂度**

`O(n)`。

**怎么想到这个方法**

先看题型识别里的信号：这题本质上就是 `频率统计 + Top-k / Frequency + Top-k`。把题目翻译成这个模板后，再去套对应的不变量、状态定义或数据结构，就会更容易写出来。

**常见 Follow-up**

- 能不能把空间复杂度再压缩一层？
- 有没有另一种常见模板也能解决这题？

## 4. Top K Frequent Words

**题型识别**

```text
频率 + 自定义排序 / Frequency + Custom Sorting
```

**题目含义**

规则是：

1. 频率高的在前
2. 频率相同则按字典序小的在前

所以排序 key 可以写成：

```text
(-count[word], word)
```

**Python 代码**

```python
from collections import Counter
from typing import List

class Solution:
    def topKFrequent(self, words: List[str], k: int) -> List[str]:
        count = Counter(words)
        return sorted(count, key=lambda w: (-count[w], w))[:k]
```

**时间复杂度**

`O(n log k)`。

**空间复杂度**

`O(n)`。

**怎么想到这个方法**

先看题型识别里的信号：这题本质上就是 `频率 + 自定义排序 / Frequency + Custom Sorting`。把题目翻译成这个模板后，再去套对应的不变量、状态定义或数据结构，就会更容易写出来。

**常见 Follow-up**

- 能不能把空间复杂度再压缩一层？
- 有没有另一种常见模板也能解决这题？

## 5. Reorganize String

**题型识别**

```text
最大堆贪心 / Greedy with Max Heap
```

**题目含义**

每次都优先取当前剩余次数最多的字符，但不能和前一个字符相同。  
所以常见做法是：

- 用最大堆按剩余频率取字符
- 上一个刚用过的字符先暂存一轮，下一轮再放回堆

**Python 代码**

```python
import heapq
from collections import Counter

class Solution:
    def reorganizeString(self, s: str) -> str:
        count = Counter(s)
        heap = [(-freq, ch) for ch, freq in count.items()]
        heapq.heapify(heap)

        prev = (0, "")
        ans = []

        while heap:
            freq, ch = heapq.heappop(heap)
            ans.append(ch)

            if prev[0] < 0:
                heapq.heappush(heap, prev)

            prev = (freq + 1, ch)

        return "".join(ans) if len(ans) == len(s) else ""
```

**时间复杂度**

常见是 `O(n log k)` 或单次 `O(log n)`。

**空间复杂度**

通常是堆大小 `O(k)` 或 `O(n)`。

**怎么想到这个方法**

先看题型识别里的信号：这题本质上就是 `最大堆贪心 / Greedy with Max Heap`。把题目翻译成这个模板后，再去套对应的不变量、状态定义或数据结构，就会更容易写出来。

**常见 Follow-up**

- 如果 `k` 很小，为什么堆通常比全排序更合适？
- 如果数据流持续到来，解法要怎么调整？

## 6. Find Median from Data Stream

**题型识别**

```text
双堆 / Two Heaps
```

**题目含义**

维护两个堆：

- `small`：大顶堆，保存较小的一半
- `large`：小顶堆，保存较大的一半

保证：

- 两边元素数平衡
- `small` 中最大值 <= `large` 中最小值

**Python 代码**

```python
import heapq

class MedianFinder:
    def __init__(self):
        self.small = []
        self.large = []

    def addNum(self, num: int) -> None:
        heapq.heappush(self.small, -num)
        heapq.heappush(self.large, -heapq.heappop(self.small))
        if len(self.large) > len(self.small):
            heapq.heappush(self.small, -heapq.heappop(self.large))

    def findMedian(self) -> float:
        if len(self.small) > len(self.large):
            return -self.small[0]
        return (-self.small[0] + self.large[0]) / 2
```

**时间复杂度**

插入 `O(log n)`，查询 `O(1)`。

**空间复杂度**

`O(n)`。

**怎么想到这个方法**

先看题型识别里的信号：这题本质上就是 `双堆 / Two Heaps`。把题目翻译成这个模板后，再去套对应的不变量、状态定义或数据结构，就会更容易写出来。

**常见 Follow-up**

- 如果 `k` 很小，为什么堆通常比全排序更合适？
- 如果数据流持续到来，解法要怎么调整？

## 7. Kth Largest Element in a Stream

**题型识别**

```text
流式 Top-k / Streaming Top-k
```

**题目含义**

和数组版第 `k` 大一样，依然维护大小为 `k` 的小顶堆。  
每次插入新元素后：

- 如果堆超出 `k` 个，弹掉最小值
- 堆顶就是当前第 `k` 大

**Python 代码**

```python
import heapq
from typing import List

class KthLargest:
    def __init__(self, k: int, nums: List[int]):
        self.k = k
        self.heap = nums
        heapq.heapify(self.heap)
        while len(self.heap) > k:
            heapq.heappop(self.heap)

    def add(self, val: int) -> int:
        heapq.heappush(self.heap, val)
        if len(self.heap) > self.k:
            heapq.heappop(self.heap)
        return self.heap[0]
```

**时间复杂度**

请结合这题使用的主循环、排序、堆或递归分支来记忆复杂度。

**空间复杂度**

请结合辅助数据结构与递归深度来判断空间复杂度。

**怎么想到这个方法**

先看题型识别里的信号：这题本质上就是 `流式 Top-k / Streaming Top-k`。把题目翻译成这个模板后，再去套对应的不变量、状态定义或数据结构，就会更容易写出来。

**常见 Follow-up**

- 能不能把空间复杂度再压缩一层？
- 有没有另一种常见模板也能解决这题？

## 8. LRU Cache

**题型识别**

```text
哈希表 + 双向链表 / Hash Map + Doubly Linked List
```

**题目含义**

这题要求：

- `get` O(1)
- `put` O(1)

所以需要两部分：

- 哈希表：`key -> node`
- 双向链表：维护最近使用顺序

最近使用的放尾部，最久未使用的在头部后面。

**Python 代码**

```python
class Node:
    def __init__(self, key=0, val=0):
        self.key = key
        self.val = val
        self.prev = None
        self.next = None

class LRUCache:
    def __init__(self, capacity: int):
        self.cap = capacity
        self.map = {}
        self.left = Node()
        self.right = Node()
        self.left.next = self.right
        self.right.prev = self.left

    def remove(self, node):
        prev, nxt = node.prev, node.next
        prev.next = nxt
        nxt.prev = prev

    def insert(self, node):
        prev = self.right.prev
        prev.next = node
        node.prev = prev
        node.next = self.right
        self.right.prev = node

    def get(self, key: int) -> int:
        if key not in self.map:
            return -1
        node = self.map[key]
        self.remove(node)
        self.insert(node)
        return node.val

    def put(self, key: int, value: int) -> None:
        if key in self.map:
            self.remove(self.map[key])

        node = Node(key, value)
        self.map[key] = node
        self.insert(node)

        if len(self.map) > self.cap:
            lru = self.left.next
            self.remove(lru)
            del self.map[lru.key]
```

**时间复杂度**

`O(1)` 均摊完成 `get/put`。

**空间复杂度**

`O(capacity)`。

**怎么想到这个方法**

先看题型识别里的信号：这题本质上就是 `哈希表 + 双向链表 / Hash Map + Doubly Linked List`。把题目翻译成这个模板后，再去套对应的不变量、状态定义或数据结构，就会更容易写出来。

**常见 Follow-up**

- 如果空间受限，能不能改成排序或双指针？
- 如果要返回所有答案而不是一个答案，如何去重？

## 9. Insert Delete GetRandom O(1)

**题型识别**

```text
数组 + 哈希表 / Array + Hash Map
```

**题目含义**

要求：

- 插入 O(1)
- 删除 O(1)
- 随机取 O(1)

所以：

- 数组支持 `random.choice`
- 哈希表记录每个值在数组中的位置

删除时把目标元素和最后一个元素交换，再弹出。

**Python 代码**

```python
import random

class RandomizedSet:
    def __init__(self):
        self.arr = []
        self.pos = {}

    def insert(self, val: int) -> bool:
        if val in self.pos:
            return False
        self.pos[val] = len(self.arr)
        self.arr.append(val)
        return True

    def remove(self, val: int) -> bool:
        if val not in self.pos:
            return False

        idx = self.pos[val]
        last = self.arr[-1]
        self.arr[idx] = last
        self.pos[last] = idx

        self.arr.pop()
        del self.pos[val]
        return True

    def getRandom(self) -> int:
        return random.choice(self.arr)
```

**时间复杂度**

`O(1)` 均摊。

**空间复杂度**

`O(n)`。

**怎么想到这个方法**

先看题型识别里的信号：这题本质上就是 `数组 + 哈希表 / Array + Hash Map`。把题目翻译成这个模板后，再去套对应的不变量、状态定义或数据结构，就会更容易写出来。

**常见 Follow-up**

- 如果空间受限，能不能改成排序或双指针？
- 如果要返回所有答案而不是一个答案，如何去重？

## 10. Implement Trie (Prefix Tree)

**题型识别**

```text
字典树 / Trie
```

**题目含义**

Trie 适合处理前缀问题。  
每个节点存：

- `children`
- 是否是单词结尾 `end`

**Python 代码**

```python
class TrieNode:
    def __init__(self):
        self.children = {}
        self.end = False

class Trie:
    def __init__(self):
        self.root = TrieNode()

    def insert(self, word: str) -> None:
        cur = self.root
        for ch in word:
            if ch not in cur.children:
                cur.children[ch] = TrieNode()
            cur = cur.children[ch]
        cur.end = True

    def search(self, word: str) -> bool:
        cur = self.root
        for ch in word:
            if ch not in cur.children:
                return False
            cur = cur.children[ch]
        return cur.end

    def startsWith(self, prefix: str) -> bool:
        cur = self.root
        for ch in prefix:
            if ch not in cur.children:
                return False
            cur = cur.children[ch]
        return True
```

**时间复杂度**

每次操作 `O(L)`。

**空间复杂度**

`O(total_chars)`。

**怎么想到这个方法**

先看题型识别里的信号：这题本质上就是 `字典树 / Trie`。把题目翻译成这个模板后，再去套对应的不变量、状态定义或数据结构，就会更容易写出来。

**常见 Follow-up**

- 能不能把空间复杂度再压缩一层？
- 有没有另一种常见模板也能解决这题？

## 11. Add and Search Word - Data structure design

**题型识别**

```text
Trie + DFS / Trie with Wildcard Search
```

**题目含义**

和普通 Trie 不同的是，这题支持 `.` 通配符。  
所以查询时：

- 普通字符：沿一个分支走
- `.`：尝试所有子节点

**Python 代码**

```python
class TrieNode:
    def __init__(self):
        self.children = {}
        self.end = False

class WordDictionary:
    def __init__(self):
        self.root = TrieNode()

    def addWord(self, word: str) -> None:
        cur = self.root
        for ch in word:
            if ch not in cur.children:
                cur.children[ch] = TrieNode()
            cur = cur.children[ch]
        cur.end = True

    def search(self, word: str) -> bool:
        def dfs(i, node):
            if i == len(word):
                return node.end

            ch = word[i]
            if ch == ".":
                return any(dfs(i + 1, child) for child in node.children.values())

            if ch not in node.children:
                return False

            return dfs(i + 1, node.children[ch])

        return dfs(0, self.root)
```

**时间复杂度**

普通查找 `O(L)`，带通配符最坏指数级。

**空间复杂度**

`O(total_chars)`。

**怎么想到这个方法**

先看题型识别里的信号：这题本质上就是 `Trie + DFS / Trie with Wildcard Search`。把题目翻译成这个模板后，再去套对应的不变量、状态定义或数据结构，就会更容易写出来。

**常见 Follow-up**

- 如果要返回具体路径而不是只判断可行性，额外记录什么？
- DFS、BFS、并查集三种解法分别适合什么场景？

## 12. Design Tic-Tac-Toe

**题型识别**

```text
计数设计 / Counting-based Design
```

**题目含义**

不需要真的维护整个棋盘再每次检查。  
只要维护：

- 每行计数
- 每列计数
- 主对角线
- 副对角线

玩家 1 记作 `+1`，玩家 2 记作 `-1`。  
某一项绝对值达到 `n` 就说明有人赢。

**Python 代码**

```python
class TicTacToe:
    def __init__(self, n: int):
        self.n = n
        self.rows = [0] * n
        self.cols = [0] * n
        self.diag = 0
        self.anti = 0

    def move(self, row: int, col: int, player: int) -> int:
        add = 1 if player == 1 else -1

        self.rows[row] += add
        self.cols[col] += add

        if row == col:
            self.diag += add
        if row + col == self.n - 1:
            self.anti += add

        if any(abs(x) == self.n for x in (self.rows[row], self.cols[col], self.diag, self.anti)):
            return player

        return 0
```

**时间复杂度**

请结合这题使用的主循环、排序、堆或递归分支来记忆复杂度。

**空间复杂度**

请结合辅助数据结构与递归深度来判断空间复杂度。

**怎么想到这个方法**

先看题型识别里的信号：这题本质上就是 `计数设计 / Counting-based Design`。把题目翻译成这个模板后，再去套对应的不变量、状态定义或数据结构，就会更容易写出来。

**常见 Follow-up**

- 能不能把空间复杂度再压缩一层？
- 有没有另一种常见模板也能解决这题？

## 13. Binary Search Tree Iterator

**题型识别**

```text
中序迭代器 / Inorder Iterator
```

**题目含义**

BST 的中序遍历是递增顺序。  
所以迭代器只要维护一条“当前路径上的左链栈”即可。

**Python 代码**

```python
class BSTIterator:
    def __init__(self, root: 'Optional[TreeNode]'):
        self.stack = []
        self.push_left(root)

    def push_left(self, node):
        while node:
            self.stack.append(node)
            node = node.left

    def next(self) -> int:
        node = self.stack.pop()
        self.push_left(node.right)
        return node.val

    def hasNext(self) -> bool:
        return bool(self.stack)
```

**时间复杂度**

`next/hasNext` 均摊 `O(1)`。

**空间复杂度**

`O(h)`。

**怎么想到这个方法**

先看题型识别里的信号：这题本质上就是 `中序迭代器 / Inorder Iterator`。把题目翻译成这个模板后，再去套对应的不变量、状态定义或数据结构，就会更容易写出来。

**常见 Follow-up**

- 能不能把空间复杂度再压缩一层？
- 有没有另一种常见模板也能解决这题？

## 14. Merge Intervals

**题型识别**

```text
区间合并 / Interval Merge
```

**题目含义**

区间题几乎都先排序。  
排序后如果当前区间和上一个区间重叠，就合并；否则开启新区间。

**Python 代码**

```python
from typing import List

class Solution:
    def merge(self, intervals: List[List[int]]) -> List[List[int]]:
        intervals.sort()
        ans = []

        for start, end in intervals:
            if not ans or ans[-1][1] < start:
                ans.append([start, end])
            else:
                ans[-1][1] = max(ans[-1][1], end)

        return ans
```

**时间复杂度**

`O(n log n)`。

**空间复杂度**

`O(n)`。

**怎么想到这个方法**

先看题型识别里的信号：这题本质上就是 `区间合并 / Interval Merge`。把题目翻译成这个模板后，再去套对应的不变量、状态定义或数据结构，就会更容易写出来。

**常见 Follow-up**

- 能不能把空间复杂度再压缩一层？
- 有没有另一种常见模板也能解决这题？

## 15. Insert Interval

**题型识别**

```text
区间插入合并 / Interval Insert and Merge
```

**题目含义**

把题目分成三段：

1. 完全在新区间左边的，直接加入
2. 和新区间重叠的，合并进 `newInterval`
3. 完全在新区间右边的，最后全部接上

**Python 代码**

```python
from typing import List

class Solution:
    def insert(self, intervals: List[List[int]], newInterval: List[int]) -> List[List[int]]:
        ans = []
        i = 0

        while i < len(intervals) and intervals[i][1] < newInterval[0]:
            ans.append(intervals[i])
            i += 1

        while i < len(intervals) and intervals[i][0] <= newInterval[1]:
            newInterval[0] = min(newInterval[0], intervals[i][0])
            newInterval[1] = max(newInterval[1], intervals[i][1])
            i += 1

        ans.append(newInterval)
        ans.extend(intervals[i:])
        return ans
```

**时间复杂度**

`O(n)`。

**空间复杂度**

`O(n)`。

**怎么想到这个方法**

先看题型识别里的信号：这题本质上就是 `区间插入合并 / Interval Insert and Merge`。把题目翻译成这个模板后，再去套对应的不变量、状态定义或数据结构，就会更容易写出来。

**常见 Follow-up**

- 能不能把空间复杂度再压缩一层？
- 有没有另一种常见模板也能解决这题？

## 16. Meeting Rooms II

**题型识别**

```text
区间 + 最小堆 / Interval Scheduling with Min Heap
```

**题目含义**

按会议开始时间排序。  
最小堆维护当前占用中的会议结束时间：

- 如果最早结束的会议已经结束，可以复用会议室
- 否则需要新开会议室

**Python 代码**

```python
import heapq
from typing import List

class Solution:
    def minMeetingRooms(self, intervals: List[List[int]]) -> int:
        intervals.sort()
        heap = []

        for start, end in intervals:
            if heap and heap[0] <= start:
                heapq.heappop(heap)
            heapq.heappush(heap, end)

        return len(heap)
```

**时间复杂度**

`O(n log n)`。

**空间复杂度**

`O(n)`。

**怎么想到这个方法**

先看题型识别里的信号：这题本质上就是 `区间 + 最小堆 / Interval Scheduling with Min Heap`。把题目翻译成这个模板后，再去套对应的不变量、状态定义或数据结构，就会更容易写出来。

**常见 Follow-up**

- 如果 `k` 很小，为什么堆通常比全排序更合适？
- 如果数据流持续到来，解法要怎么调整？

## 17. Pow(x, n)

**题型识别**

```text
快速幂 / Fast Power
```

**题目含义**

利用指数拆半：

```text
x^n = (x^(n/2))^2
```

这样时间复杂度从 `O(n)` 降到 `O(log n)`。

**Python 代码**

```python
class Solution:
    def myPow(self, x: float, n: int) -> float:
        def fast_pow(a, b):
            if b == 0:
                return 1
            half = fast_pow(a, b // 2)
            return half * half if b % 2 == 0 else half * half * a

        return fast_pow(x, n) if n >= 0 else 1 / fast_pow(x, -n)
```

**时间复杂度**

`O(log n)`。

**空间复杂度**

`O(log n)` 递归版或 `O(1)` 迭代版。

**怎么想到这个方法**

先看题型识别里的信号：这题本质上就是 `快速幂 / Fast Power`。把题目翻译成这个模板后，再去套对应的不变量、状态定义或数据结构，就会更容易写出来。

**常见 Follow-up**

- 能不能把空间复杂度再压缩一层？
- 有没有另一种常见模板也能解决这题？

## 18. Divide Two Integers

**题型识别**

```text
位运算倍增 / Bit Doubling
```

**题目含义**

不能用乘除法时，可以不断把除数翻倍，找当前能减掉的最大块。  
本质像“二进制拆分”。

**Python 代码**

```python
class Solution:
    def divide(self, dividend: int, divisor: int) -> int:
        sign = -1 if (dividend < 0) ^ (divisor < 0) else 1
        dividend, divisor = abs(dividend), abs(divisor)
        ans = 0

        while dividend >= divisor:
            temp, multiple = divisor, 1
            while dividend >= (temp << 1):
                temp <<= 1
                multiple <<= 1
            dividend -= temp
            ans += multiple

        ans *= sign
        return min(max(-2**31, ans), 2**31 - 1)
```

**时间复杂度**

`O(log(dividend))`。

**空间复杂度**

`O(1)`。

**怎么想到这个方法**

先看题型识别里的信号：这题本质上就是 `位运算倍增 / Bit Doubling`。把题目翻译成这个模板后，再去套对应的不变量、状态定义或数据结构，就会更容易写出来。

**常见 Follow-up**

- 能不能把空间复杂度再压缩一层？
- 有没有另一种常见模板也能解决这题？

## 19. Rotate Image

**题型识别**

```text
矩阵原地旋转 / In-place Matrix Rotation
```

**题目含义**

顺时针旋转 90 度的经典做法：

1. 先沿主对角线转置
2. 再把每一行反转

**Python 代码**

```python
from typing import List

class Solution:
    def rotate(self, matrix: List[List[int]]) -> None:
        n = len(matrix)

        for i in range(n):
            for j in range(i + 1, n):
                matrix[i][j], matrix[j][i] = matrix[j][i], matrix[i][j]

        for row in matrix:
            row.reverse()
```

**时间复杂度**

`O(n^2)`。

**空间复杂度**

`O(1)`。

**怎么想到这个方法**

先看题型识别里的信号：这题本质上就是 `矩阵原地旋转 / In-place Matrix Rotation`。把题目翻译成这个模板后，再去套对应的不变量、状态定义或数据结构，就会更容易写出来。

**常见 Follow-up**

- 能不能把空间复杂度再压缩一层？
- 有没有另一种常见模板也能解决这题？

## 20. Spiral Matrix

**题型识别**

```text
边界收缩 / Shrinking Boundaries
```

**题目含义**

维护四个边界：

- `top`
- `bottom`
- `left`
- `right`

按顺时针一圈一圈遍历，每走完一边就收缩对应边界。

**Python 代码**

```python
from typing import List

class Solution:
    def spiralOrder(self, matrix: List[List[int]]) -> List[int]:
        ans = []
        top, bottom = 0, len(matrix) - 1
        left, right = 0, len(matrix[0]) - 1

        while top <= bottom and left <= right:
            for c in range(left, right + 1):
                ans.append(matrix[top][c])
            top += 1

            for r in range(top, bottom + 1):
                ans.append(matrix[r][right])
            right -= 1

            if top <= bottom:
                for c in range(right, left - 1, -1):
                    ans.append(matrix[bottom][c])
                bottom -= 1

            if left <= right:
                for r in range(bottom, top - 1, -1):
                    ans.append(matrix[r][left])
                left += 1

        return ans
```

**时间复杂度**

`O(mn)`。

**空间复杂度**

`O(1)` 额外，不计答案。

**怎么想到这个方法**

先看题型识别里的信号：这题本质上就是 `边界收缩 / Shrinking Boundaries`。把题目翻译成这个模板后，再去套对应的不变量、状态定义或数据结构，就会更容易写出来。

**常见 Follow-up**

- 能不能把空间复杂度再压缩一层？
- 有没有另一种常见模板也能解决这题？

## 21. Set Matrix Zeroes

**题型识别**

```text
矩阵原地标记 / In-place Matrix Marking
```

**题目含义**

为了做到 `O(1)` 额外空间，可以把第一行和第一列当标记位。  
先记录第一行第一列是否本来要清零，再用它们标记其他行列。

**Python 代码**

```python
from typing import List

class Solution:
    def setZeroes(self, matrix: List[List[int]]) -> None:
        m, n = len(matrix), len(matrix[0])
        row0 = any(matrix[0][c] == 0 for c in range(n))
        col0 = any(matrix[r][0] == 0 for r in range(m))

        for r in range(1, m):
            for c in range(1, n):
                if matrix[r][c] == 0:
                    matrix[r][0] = matrix[0][c] = 0

        for r in range(1, m):
            if matrix[r][0] == 0:
                for c in range(1, n):
                    matrix[r][c] = 0

        for c in range(1, n):
            if matrix[0][c] == 0:
                for r in range(1, m):
                    matrix[r][c] = 0

        if row0:
            for c in range(n):
                matrix[0][c] = 0
        if col0:
            for r in range(m):
                matrix[r][0] = 0
```

**时间复杂度**

`O(mn)`。

**空间复杂度**

`O(1)`。

**怎么想到这个方法**

先看题型识别里的信号：这题本质上就是 `矩阵原地标记 / In-place Matrix Marking`。把题目翻译成这个模板后，再去套对应的不变量、状态定义或数据结构，就会更容易写出来。

**常见 Follow-up**

- 能不能把空间复杂度再压缩一层？
- 有没有另一种常见模板也能解决这题？

## 22. String to Integer (atoi)

**题型识别**

```text
字符串解析 / String Parsing
```

**题目含义**

按顺序处理：

1. 跳过前导空格
2. 处理正负号
3. 读取数字
4. 截断到 32 位有符号整数范围

**Python 代码**

```python
class Solution:
    def myAtoi(self, s: str) -> int:
        i = 0
        while i < len(s) and s[i] == " ":
            i += 1

        sign = 1
        if i < len(s) and s[i] in "+-":
            sign = -1 if s[i] == "-" else 1
            i += 1

        ans = 0
        while i < len(s) and s[i].isdigit():
            ans = ans * 10 + int(s[i])
            i += 1

        ans *= sign
        return min(max(ans, -2**31), 2**31 - 1)
```

**时间复杂度**

`O(n)`。

**空间复杂度**

`O(1)`。

**怎么想到这个方法**

先看题型识别里的信号：这题本质上就是 `字符串解析 / String Parsing`。把题目翻译成这个模板后，再去套对应的不变量、状态定义或数据结构，就会更容易写出来。

**常见 Follow-up**

- 能不能把空间复杂度再压缩一层？
- 有没有另一种常见模板也能解决这题？

## 23. Roman to Integer

**题型识别**

```text
规则扫描 / Rule-based Scan
```

**题目含义**

罗马数字里，如果当前字符比后一个小，说明它是减法，应该减掉；否则加上。

**Python 代码**

```python
class Solution:
    def romanToInt(self, s: str) -> int:
        mp = {"I":1,"V":5,"X":10,"L":50,"C":100,"D":500,"M":1000}
        ans = 0

        for i, ch in enumerate(s):
            if i + 1 < len(s) and mp[ch] < mp[s[i + 1]]:
                ans -= mp[ch]
            else:
                ans += mp[ch]

        return ans
```

**时间复杂度**

`O(n)`。

**空间复杂度**

`O(1)`。

**怎么想到这个方法**

先看题型识别里的信号：这题本质上就是 `规则扫描 / Rule-based Scan`。把题目翻译成这个模板后，再去套对应的不变量、状态定义或数据结构，就会更容易写出来。

**常见 Follow-up**

- 能不能把空间复杂度再压缩一层？
- 有没有另一种常见模板也能解决这题？

## 24. First Missing Positive

**题型识别**

```text
原地下标映射 / In-place Index Placement
```

**题目含义**

如果数组长度是 `n`，那么答案一定在 `1..n+1` 之间。  
理想状态是把数字 `x` 放到下标 `x-1` 上。  
最后第一个不满足 `nums[i] == i+1` 的位置就是答案。

**Python 代码**

```python
from typing import List

class Solution:
    def firstMissingPositive(self, nums: List[int]) -> int:
        n = len(nums)

        for i in range(n):
            while 1 <= nums[i] <= n and nums[nums[i] - 1] != nums[i]:
                j = nums[i] - 1
                nums[i], nums[j] = nums[j], nums[i]

        for i in range(n):
            if nums[i] != i + 1:
                return i + 1

        return n + 1
```

**时间复杂度**

`O(n)`。

**空间复杂度**

`O(1)`。

**怎么想到这个方法**

先看题型识别里的信号：这题本质上就是 `原地下标映射 / In-place Index Placement`。把题目翻译成这个模板后，再去套对应的不变量、状态定义或数据结构，就会更容易写出来。

**常见 Follow-up**

- 能不能把空间复杂度再压缩一层？
- 有没有另一种常见模板也能解决这题？

## 25. Longest Consecutive Sequence

**题型识别**

```text
哈希集合 / Hash Set
```

**题目含义**

只有当 `x - 1` 不存在时，`x` 才可能是一个连续序列的起点。  
从起点开始不断往后数，就能得到该序列长度。

**Python 代码**

```python
from typing import List

class Solution:
    def longestConsecutive(self, nums: List[int]) -> int:
        s = set(nums)
        ans = 0

        for x in s:
            if x - 1 not in s:
                y = x
                while y in s:
                    y += 1
                ans = max(ans, y - x)

        return ans
```

**时间复杂度**

`O(n)` 平均。

**空间复杂度**

`O(n)`。

**怎么想到这个方法**

先看题型识别里的信号：这题本质上就是 `哈希集合 / Hash Set`。把题目翻译成这个模板后，再去套对应的不变量、状态定义或数据结构，就会更容易写出来。

**常见 Follow-up**

- 如果空间受限，能不能改成排序或双指针？
- 如果要返回所有答案而不是一个答案，如何去重？

## 26. Palindrome Permutation

**题型识别**

```text
频率统计 / Frequency Counting
```

**题目含义**

一个字符串能重排成回文，当且仅当：

- 最多只有一个字符出现奇数次

**Python 代码**

```python
from collections import Counter

class Solution:
    def canPermutePalindrome(self, s: str) -> bool:
        return sum(v % 2 for v in Counter(s).values()) <= 1
```

**时间复杂度**

请结合这题使用的主循环、排序、堆或递归分支来记忆复杂度。

**空间复杂度**

请结合辅助数据结构与递归深度来判断空间复杂度。

**怎么想到这个方法**

先看题型识别里的信号：这题本质上就是 `频率统计 / Frequency Counting`。把题目翻译成这个模板后，再去套对应的不变量、状态定义或数据结构，就会更容易写出来。

**常见 Follow-up**

- 能不能把空间复杂度再压缩一层？
- 有没有另一种常见模板也能解决这题？

## 27. Integer to English Words

**题型识别**

```text
递归分段 / Recursive Number Decomposition
```

**题目含义**

把数字按：

- Billion
- Million
- Thousand
- Hundred

一段一段拆开处理。  
每一段都可以递归转换成英文。

**Python 代码**

```python
class Solution:
    def numberToWords(self, num: int) -> str:
        if num == 0:
            return "Zero"

        below_20 = [
            "", "One", "Two", "Three", "Four", "Five", "Six", "Seven", "Eight", "Nine", "Ten",
            "Eleven", "Twelve", "Thirteen", "Fourteen", "Fifteen", "Sixteen", "Seventeen", "Eighteen", "Nineteen"
        ]
        tens = ["", "", "Twenty", "Thirty", "Forty", "Fifty", "Sixty", "Seventy", "Eighty", "Ninety"]

        def helper(n):
            if n == 0:
                return ""
            if n < 20:
                return below_20[n] + " "
            if n < 100:
                return tens[n // 10] + " " + helper(n % 10)
            return below_20[n // 100] + " Hundred " + helper(n % 100)

        units = [
            (10**9, "Billion"),
            (10**6, "Million"),
            (1000, "Thousand"),
            (1, ""),
        ]

        ans = ""
        for value, name in units:
            if num >= value:
                ans += helper(num // value) + name + " "
                num %= value

        return " ".join(ans.split())
```

**时间复杂度**

请结合这题使用的主循环、排序、堆或递归分支来记忆复杂度。

**空间复杂度**

请结合辅助数据结构与递归深度来判断空间复杂度。

**怎么想到这个方法**

先看题型识别里的信号：这题本质上就是 `递归分段 / Recursive Number Decomposition`。把题目翻译成这个模板后，再去套对应的不变量、状态定义或数据结构，就会更容易写出来。

**常见 Follow-up**

- 能不能把空间复杂度再压缩一层？
- 有没有另一种常见模板也能解决这题？

## 28. First Unique Character in a String

**题型识别**

```text
频率统计 / Frequency Count
```

**题目含义**

先统计每个字符出现次数，再按原顺序找第一个频率为 1 的字符。

**Python 代码**

```python
from collections import Counter

class Solution:
    def firstUniqChar(self, s: str) -> int:
        count = Counter(s)
        for i, ch in enumerate(s):
            if count[ch] == 1:
                return i
        return -1
```

**时间复杂度**

请结合这题使用的主循环、排序、堆或递归分支来记忆复杂度。

**空间复杂度**

请结合辅助数据结构与递归深度来判断空间复杂度。

**怎么想到这个方法**

先看题型识别里的信号：这题本质上就是 `频率统计 / Frequency Count`。把题目翻译成这个模板后，再去套对应的不变量、状态定义或数据结构，就会更容易写出来。

**常见 Follow-up**

- 能不能把空间复杂度再压缩一层？
- 有没有另一种常见模板也能解决这题？

## 29. Validate IP Address

**题型识别**

```text
规则校验 / Rule-based Validation
```

**题目含义**

分别写：

- IPv4 校验函数
- IPv6 校验函数

然后按规则判断即可。

**Python 代码**

```python
class Solution:
    def validIPAddress(self, queryIP: str) -> str:
        def is_ipv4():
            parts = queryIP.split(".")
            if len(parts) != 4:
                return False
            for p in parts:
                if not p or (len(p) > 1 and p[0] == "0") or not p.isdigit():
                    return False
                if not 0 <= int(p) <= 255:
                    return False
            return True

        def is_ipv6():
            parts = queryIP.split(":")
            hexdigits = "0123456789abcdefABCDEF"
            if len(parts) != 8:
                return False
            for p in parts:
                if not 1 <= len(p) <= 4:
                    return False
                if any(ch not in hexdigits for ch in p):
                    return False
            return True

        if is_ipv4():
            return "IPv4"
        if is_ipv6():
            return "IPv6"
        return "Neither"
```

**时间复杂度**

请结合这题使用的主循环、排序、堆或递归分支来记忆复杂度。

**空间复杂度**

请结合辅助数据结构与递归深度来判断空间复杂度。

**怎么想到这个方法**

先看题型识别里的信号：这题本质上就是 `规则校验 / Rule-based Validation`。把题目翻译成这个模板后，再去套对应的不变量、状态定义或数据结构，就会更容易写出来。

**常见 Follow-up**

- 能不能把空间复杂度再压缩一层？
- 有没有另一种常见模板也能解决这题？

## 30. Task Scheduler

**题型识别**

```text
贪心计数 / Greedy Counting
```

**题目含义**

最频繁任务决定了骨架长度。  
公式：

```text
(max_freq - 1) * (n + 1) + count_of_max_freq
```

但最终答案不能小于任务总数。

**Python 代码**

```python
from collections import Counter
from typing import List

class Solution:
    def leastInterval(self, tasks: List[str], n: int) -> int:
        freq = Counter(tasks).values()
        maxf = max(freq)
        max_count = sum(1 for f in freq if f == maxf)
        return max(len(tasks), (maxf - 1) * (n + 1) + max_count)
```

**时间复杂度**

`O(n)` 到 `O(n log k)`，取决于实现。

**空间复杂度**

`O(1)` 到 `O(k)`。

**怎么想到这个方法**

先看题型识别里的信号：这题本质上就是 `贪心计数 / Greedy Counting`。把题目翻译成这个模板后，再去套对应的不变量、状态定义或数据结构，就会更容易写出来。

**常见 Follow-up**

- 能不能把空间复杂度再压缩一层？
- 有没有另一种常见模板也能解决这题？

## 31. Pancake Sorting

**题型识别**

```text
模拟排序 / Simulation Sorting
```

**题目含义**

每次把当前最大值翻到最前面，再翻到它应该去的位置。  
从大到小依次放好。

**Python 代码**

```python
from typing import List

class Solution:
    def pancakeSort(self, arr: List[int]) -> List[int]:
        ans = []
        n = len(arr)

        for size in range(n, 1, -1):
            idx = arr.index(size)
            if idx == size - 1:
                continue
            if idx != 0:
                ans.append(idx + 1)
                arr[:idx + 1] = reversed(arr[:idx + 1])
            ans.append(size)
            arr[:size] = reversed(arr[:size])

        return ans
```

**时间复杂度**

请结合这题使用的主循环、排序、堆或递归分支来记忆复杂度。

**空间复杂度**

请结合辅助数据结构与递归深度来判断空间复杂度。

**怎么想到这个方法**

先看题型识别里的信号：这题本质上就是 `模拟排序 / Simulation Sorting`。把题目翻译成这个模板后，再去套对应的不变量、状态定义或数据结构，就会更容易写出来。

**常见 Follow-up**

- 能不能把空间复杂度再压缩一层？
- 有没有另一种常见模板也能解决这题？

## 32. Sudoku Solver

**题型识别**

```text
回溯 + 约束检查 / Backtracking with Constraints
```

**题目含义**

维护三类约束：

- 每行已有数字
- 每列已有数字
- 每个九宫格已有数字

回溯时给空格尝试填入 `1..9`，如果合法就继续。

**Python 代码**

```python
from typing import List

class Solution:
    def solveSudoku(self, board: List[List[str]]) -> None:
        rows = [set() for _ in range(9)]
        cols = [set() for _ in range(9)]
        boxes = [set() for _ in range(9)]
        empties = []

        for r in range(9):
            for c in range(9):
                if board[r][c] == ".":
                    empties.append((r, c))
                else:
                    v = board[r][c]
                    rows[r].add(v)
                    cols[c].add(v)
                    boxes[(r // 3) * 3 + c // 3].add(v)

        def backtrack(i: int) -> bool:
            if i == len(empties):
                return True

            r, c = empties[i]
            b = (r // 3) * 3 + c // 3

            for ch in "123456789":
                if ch in rows[r] or ch in cols[c] or ch in boxes[b]:
                    continue

                board[r][c] = ch
                rows[r].add(ch)
                cols[c].add(ch)
                boxes[b].add(ch)

                if backtrack(i + 1):
                    return True

                board[r][c] = "."
                rows[r].remove(ch)
                cols[c].remove(ch)
                boxes[b].remove(ch)

            return False

        backtrack(0)
```

**时间复杂度**

最坏指数级回溯。

**空间复杂度**

`O(81)` 递归深度上界。

**怎么想到这个方法**

先看题型识别里的信号：这题本质上就是 `回溯 + 约束检查 / Backtracking with Constraints`。把题目翻译成这个模板后，再去套对应的不变量、状态定义或数据结构，就会更容易写出来。

**常见 Follow-up**

- 哪些剪枝最值得优先做？
- 如果只要求返回数量而不是全部方案，代码能如何简化？

